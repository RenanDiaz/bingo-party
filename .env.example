# PartyKit Server Configuration
# ==============================
# PartyKit is a separate service that needs to be deployed independently.
#
# For local development:
#   1. Run `npm run dev:party` to start the PartyKit server
#   2. Run `npm run dev` to start the SvelteKit frontend
#   Or use `npm run dev:all` to run both concurrently
#
# For production (Vercel + PartyKit):
#   1. Deploy PartyKit: `npm run deploy:party`
#   2. Set this environment variable in Vercel to your PartyKit URL
#
# Development (default - works with `npm run dev:party`)
VITE_PARTYKIT_HOST=localhost:1999

# Production example (replace with your actual PartyKit URL after running `npm run deploy:party`)
# VITE_PARTYKIT_HOST=bingoparty.YOUR-USERNAME.partykit.dev
